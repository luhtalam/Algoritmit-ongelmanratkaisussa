#include <iostream>

using namespace std;

int t[2000001];

int main() {
    cin.sync_with_stdio(0);
    string A,B;
    cin >> A >> B;
    string C = B+'#'+A;
    int c = C.length();
    int x = 0,y= 0;
    for (int i = 0; i < c; i++) {
        
    }
    return 0;
}
